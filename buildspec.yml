version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm install
    pre_build:
      commands:
        - npm run lint
    build:
      commands:
        - ls -la
        - pwd
        - npm run build
    post_build:
      commands:
        - ls -la
        - pwd
        - echo "do nothing"
artifacts:
  files:
    - '**/*'
  base-directory: dist
  discard-paths: no
